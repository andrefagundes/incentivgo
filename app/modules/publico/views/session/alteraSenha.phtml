<section class="components">
    <div class="container margin">
        <div class="row col-sm-offset-4">
            <h6><strong> Alterar Senha.</strong></h6>
            {{ form('session/alteraSenha','class': 'form-horizontal form-login','role': 'form', 'autocomplete': 'off') }}
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                        {{ content() }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                        {{ form.render('password') }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                         {{ form.render("confirmPassword") }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                        {{ form.render('go') }}
                    </div>
                </div>
                {{ form.render('csrf') }}
            </form>
        </div>
    </div>
</section>