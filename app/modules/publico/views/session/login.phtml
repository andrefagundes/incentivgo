<section class="components">
    <div class="container margin">
       
        <div class="row col-sm-offset-4 col-lg-3 col-sm-3 col-lg-offset-4 text-center">
                {{image( 'img/users/'~logo, 'class':'img-thumbnail text-center','width':'100px','height':'100px') }}
        </div>
        <div class="row col-sm-offset-4 col-lg-offset-4">
            {{ form('class': 'form-horizontal form-login','role': 'form') }}
            <div class="form-group">
                <div class="col-sm-5 col-lg-5">
<!--                    {{ link_to("session/enviarSugestao", 'class':'btn btn-link',
                    "Sua empresa não está no Incentiv?<p> Envie uma sugestão para participar!!!</p>") }}-->
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-5 col-lg-5">
                    <?php echo $this->flashSession->output(); ?>
                    {{ content() }}
                </div>
            </div>
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                        {{ form.render('email') }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                        {{ form.render('password') }}
                        {{ form.messages('password') }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                        {{ link_to("session/esqueceuSenha", 'class':'btn btn-link', "Esqueceu a senha?") }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-5 col-lg-5">
                        {{ form.render('go') }}
                        {{ form.render('remember') }}
                            {{ form.label('remember') }}
                    </div>
                </div>
                {{ form.render('csrf') }}
            {{endform}}
        </div>
    </div>
</section>